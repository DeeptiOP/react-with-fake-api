services:
  - type: web
    name: online-store-backend
    env: node
    plan: free
    region: oregon
    root: server

    # Install dependencies in server directory
    buildCommand: npm install
    startCommand: node server.js

    envVars:
      - key: MONGODB_URI
        prompt: false
      - key: JWT_SECRET
        prompt: false
      - key: JWT_EXPIRE
        value: 7d
      - key: NODE_ENV
        value: production
      - key: BCRYPT_ROUNDS
        value: "10"
      - key: CORS_ORIGIN
        value: https://react-with-fake-api.netlify.app
